<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image Viewer" Height="400" Width="600"> <!-- Drop="ImageDrop" *** Se puede habilitar este enlace en lugar del habilitado en el código CS, cualquiera sería válido. Lo destaco porque me he fijado en ello a raíz de este proyecto. -->
        
    <Grid>
        <Menu>
            <MenuItem Header="Archivo">
                <Separator/>
                <MenuItem Name="RecientesMenu" Header="Recientes">
                    <!-- Aquí van los elementos de la lista de recientes añadidos de forma dinámica por el método correspondiente. -->
                </MenuItem>
            </MenuItem>
        </Menu>
        <Border Name="imageContainer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Image Name="displayedImage" Stretch="Uniform"/>
        </Border>
    </Grid>
</Window>